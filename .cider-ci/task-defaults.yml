# make sure it runs fast, since it doesnt need much powers
priority: 42
task_defaults:
  priority: 42

  # pick a trusted executor that has access to target server
  traits: { 'g2016': yes }

  environment_variables:
    # hosting config
    PORT: "22"
    USER: "root"
    SSHKEY: "/home/ci-exec-user/.ssh/zhdk_ci_executor_rsa"
